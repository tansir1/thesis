def jdkHome = System.getenv("JAVA_HOME")

subprojects {
    apply plugin: 'java'

	targetCompatibility = "1.8"
	sourceCompatibility = "1.8"


    repositories {
       jcenter()
    }

    version = '0.0.0'
}